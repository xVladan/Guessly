# Copy this file to ".env" in the same folder (backend/.env) and fill in a
# real value. docker-compose reads .env automatically; it's gitignored so the
# real key never ends up in the repo.
ADMIN_API_KEY=replace-with-a-long-random-string

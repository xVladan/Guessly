// Single place to point the frontend at the backend. Change this one line
// when moving from local dev to a deployed API (e.g. a production subdomain).
const GUESSLY_CONFIG = {
  apiBaseUrl: "http://localhost:5000",
  hubUrl: "http://localhost:5000/hub/game",
};
